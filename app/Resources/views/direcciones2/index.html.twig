{% extends 'base.html.twig' %}

{% block body %}
    <h1>Direcciones2s list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Ciudad</th>
                <th>Delegacion</th>
                <th>Calle</th>
                <th>Numero</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for direcciones2 in direcciones2s %}
            <tr>
                <td><a href="{{ path('direcciones2_show', { 'id': direcciones2.id }) }}">{{ direcciones2.id }}</a></td>
                <td>{{ direcciones2.ciudad }}</td>
                <td>{{ direcciones2.delegacion }}</td>
                <td>{{ direcciones2.calle }}</td>
                <td>{{ direcciones2.numero }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('direcciones2_show', { 'id': direcciones2.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('direcciones2_edit', { 'id': direcciones2.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('direcciones2_new') }}">Create a new direcciones2</a>
        </li>
    </ul>
{% endblock %}
